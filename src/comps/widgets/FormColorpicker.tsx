import React from 'react';

export function FormColorpicker(props: { name: string; value: string; onChange: (color: string) => void }) {
    return (
        <label className="block m-3">
            {props.name}
            TODO PUT COLORPICKER HERE!
        </label>
    );
}
